---
// Hero.astro
const title = "Computer Repairs and Supply";
const subtitle = "Expert IT Support in Romford & Havering";
const description =
  "With over 20 years of experience, Chiltern Computers provides expert computer repairs, custom-built gaming PCs, and reliable IT support throughout Harold Hill, Romford, and Havering. Fast, affordable, and professional service you can trust.";

const ctaButtons = [
  {
    text: "Call Now: 07971 331814",
    href: "tel:+447971331814",
    bgColor: "bg-blue-600 hover:bg-blue-700",
    icon: "phone"
  },
  {
    text: "WhatsApp Us",
    href: "https://wa.me/447971331814",
    bgColor: "bg-green-500 hover:bg-green-600",
    icon: "whatsapp"
  },
  {
    text: "Join Facebook Group",
    href: "https://www.facebook.com/groups/217286604958495",
    bgColor: "bg-blue-500 hover:bg-blue-600",
    icon: "facebook"
  },
];

const services = [
  { title: "Computer Repairs", desc: "Fast diagnosis & repair", icon: "🔧" },
  { title: "Custom PCs", desc: "Gaming & business builds", icon: "🖥️" },
  { title: "IT Support", desc: "Ongoing technical support", icon: "🛠️" }
];
---

<section    
  class="relative bg-gray-900 py-16 px-4 text-white min-h-[500px] overflow-hidden"   
  style="background: linear-gradient(135deg, rgba(15, 23, 42, 0.9) 0%, rgba(30, 41, 59, 0.8) 50%, rgba(0, 0, 0, 0.7) 100%), url('/mainpcpic.jpg'); background-size: cover; background-position: center; background-attachment: fixed;"
>
  <!-- Simplified Background Effects -->
  <div class="absolute inset-0 opacity-20" aria-hidden="true">
    <div class="floating-orb floating-orb--blue"></div>
    <div class="floating-orb floating-orb--indigo"></div>
    <div class="floating-orb floating-orb--purple"></div>
  </div>

  <div class="relative z-10 max-w-6xl mx-auto flex flex-col items-center justify-center text-center gap-8 min-h-[500px]">
    
    <!-- Trust Badge -->
    <div class="trust-badge animate-fade-in">
      <span class="text-sm font-medium text-blue-200">⭐ Trusted by 1000+ customers • 20+ Years Experience</span>
    </div>

    <!-- Main Content -->
    <div class="space-y-8 w-full">
      
      <!-- Title Section -->
      <div class="space-y-4">
        <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-black leading-tight animate-slide-in">
          <span class="gradient-text block mb-2">{title}</span>
          <span class="text-2xl sm:text-3xl md:text-4xl text-blue-200 font-bold animate-slide-in-delayed">
            {subtitle}
          </span>
        </h1>
        
        <div class="animate-fade-in-delayed">
          <div class="w-24 h-1 bg-gradient-to-r from-blue-400 via-blue-500 to-indigo-500 rounded-full mx-auto animate-glow"></div>
        </div>
      </div>

      <!-- Description -->
      <div class="animate-fade-in-delayed">
        <p class="text-lg sm:text-xl md:text-2xl max-w-4xl mx-auto leading-relaxed text-gray-100 font-light mb-6">
          {description}
        </p>
        
        <!-- Key Features -->
        <div class="flex flex-wrap justify-center gap-4 sm:gap-6 text-sm text-blue-200">
          <div class="feature-badge">
            <div class="pulse-dot"></div>
            <span>Same-day Service</span>
          </div>
          <div class="feature-badge">
            <div class="pulse-dot pulse-dot--delayed"></div>
            <span>Free Diagnosis</span>
          </div>
          <div class="feature-badge">
            <div class="pulse-dot pulse-dot--delayed-2"></div>
            <span>No Fix, No Fee</span>
          </div>
        </div>
      </div>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center animate-slide-up">
        {ctaButtons.map((btn, index) => (
          <a
            href={btn.href}
            class={`cta-button ${btn.bgColor}`}
            target={btn.href.startsWith("http") ? "_blank" : "_self"}
            rel={btn.href.startsWith("http") ? "noopener noreferrer" : undefined}
            style={`animation-delay: ${0.3 + (index * 0.1)}s;`}
          >
            <div class="cta-button__shine"></div>
            
            <div class="relative flex items-center justify-center gap-2">
              {btn.icon === "phone" && (
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                </svg>
              )}
              {btn.icon === "whatsapp" && (
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893A11.821 11.821 0 0020.885 3.608"></path>
                </svg>
              )}
              {btn.icon === "facebook" && (
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"></path>
                </svg>
              )}
              <span>{btn.text}</span>
            </div>
          </a>
        ))}
      </div>

      <!-- Service Highlights -->
      <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto animate-slide-up">
        {services.map((service, index) => (
          <div class="service-card" style={`animation-delay: ${0.5 + (index * 0.1)}s;`}>
            <div class="text-3xl mb-3 group-hover:animate-bounce">{service.icon}</div>
            <h3 class="font-bold text-lg mb-2 text-blue-100">{service.title}</h3>
            <p class="text-sm text-blue-200">{service.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  /* Core Animations */
  @keyframes slide-in {
    from { transform: translateX(-30px); opacity: 0; }
    to { transform: translateX(0); opacity: 1; }
  }
  
  @keyframes slide-up {
    from { transform: translateY(30px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
  }
  
  @keyframes fade-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
  }
  
  @keyframes glow {
    0%, 100% { box-shadow: 0 0 5px rgba(59, 130, 246, 0.5); }
    50% { box-shadow: 0 0 20px rgba(59, 130, 246, 0.8); }
  }
  
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }
  
  @keyframes shine {
    0% { transform: translateX(-100%) skewX(-12deg); }
    100% { transform: translateX(300%) skewX(-12deg); }
  }

  /* Animation Classes */
  .animate-slide-in {
    animation: slide-in 0.8s ease-out;
  }
  
  .animate-slide-in-delayed {
    animation: slide-in 0.8s ease-out 0.2s both;
  }
  
  .animate-slide-up {
    animation: slide-up 0.8s ease-out 0.4s both;
  }
  
  .animate-fade-in {
    animation: fade-in 0.8s ease-out;
  }
  
  .animate-fade-in-delayed {
    animation: fade-in 0.8s ease-out 0.3s both;
  }
  
  .animate-glow {
    animation: glow 2s ease-in-out infinite;
  }

  /* Component Styles */
  .trust-badge {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    padding: 0.5rem 1rem;
    border-radius: 9999px;
  }

  .gradient-text {
    background: linear-gradient(135deg, #60a5fa, #3b82f6, #1e40af);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .feature-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .pulse-dot {
    width: 0.5rem;
    height: 0.5rem;
    background-color: #34d399;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  .pulse-dot--delayed {
    animation-delay: 0.5s;
  }

  .pulse-dot--delayed-2 {
    animation-delay: 1s;
  }

  .cta-button {
    position: relative;
    padding: 1rem 2rem;
    border-radius: 0.75rem;
    font-weight: 700;
    transition: all 0.3s ease;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.2);
    overflow: hidden;
    transform: translateY(20px);
    opacity: 0;
    animation: slide-up 0.8s ease-out forwards;
    text-decoration: none;
  }

  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 15px 35px rgba(59, 130, 246, 0.4);
  }

  .cta-button__shine {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: none;
  }

  .cta-button:hover .cta-button__shine {
    animation: shine 0.7s ease-in-out;
  }

  .service-card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    padding: 1.5rem;
    border-radius: 1rem;
    text-align: center;
    transition: transform 0.3s ease;
    transform: translateY(20px);
    opacity: 0;
    animation: slide-up 0.8s ease-out forwards;
  }

  .service-card:hover {
    transform: translateY(-5px);
  }

  /* Floating Orbs */
  .floating-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(40px);
    animation: float 4s ease-in-out infinite;
  }

  .floating-orb--blue {
    top: 10%;
    left: 10%;
    width: 8rem;
    height: 8rem;
    background-color: #3b82f6;
  }

  .floating-orb--indigo {
    bottom: 10%;
    right: 10%;
    width: 10rem;
    height: 10rem;
    background-color: #6366f1;
    animation-delay: 1.5s;
  }

  .floating-orb--purple {
    top: 50%;
    left: 20%;
    width: 6rem;
    height: 6rem;
    background-color: #8b5cf6;
    animation-delay: 3s;
  }

  /* Responsive Improvements */
  @media (max-width: 640px) {
    .cta-button {
      padding: 0.875rem 1.5rem;
      font-size: 0.875rem;
    }
    
    .service-card {
      padding: 1rem;
    }
    
    .feature-badge {
      font-size: 0.75rem;
    }
  }

  /* Reduced Motion Support */
  @media (prefers-reduced-motion: reduce) {
    * {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
    
    .floating-orb {
      animation: none;
    }
  }
</style>